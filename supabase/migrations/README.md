# M2 migrations

Two version-controlled CLI-created SQL migrations implement the schema, policies and private bucket. PostgreSQL tests pass. They are not yet applied to a hosted Supabase project. See `docs/08-m2-handoff.md` for the acceptance gaps and setup.
